## 1. Implementation
- [x] 1.1 Add `line-clamp-3` to the markdown container `div` in `landing/src/components/molecules/CourseCard.astro`.
- [x] 1.2 Run `npm run build` inside `landing` to ensure no build regressions.
- [x] 1.3 Visually confirm the description is truncated at 3 lines without breaking markdown rendering in the browser.
